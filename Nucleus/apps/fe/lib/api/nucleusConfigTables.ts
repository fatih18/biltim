/**
 * The backend's own config, re-exported so the API seam can read a table's
 * columns without importing across the app boundary twice.
 */
import nucleusConfig from '../../../be-nucleus/config.json'

export default nucleusConfig
